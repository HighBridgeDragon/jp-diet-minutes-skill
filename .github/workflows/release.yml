name: Release

on:
  push:
    tags:
      - 'v*'
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Validate skill against Agent Skills spec
        run: bash .github/scripts/validate-skill.sh jp-diet-minutes

  release:
    needs: validate
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Build skill zip
        run: |
          # MIT は複製物に許諾表示を含めることを求めるため、zip 内にも LICENSE を同梱する
          cp LICENSE jp-diet-minutes/LICENSE
          # 展開時のトップが jp-diet-minutes/ になるよう、ディレクトリごと固める
          zip -r jp-diet-minutes.zip jp-diet-minutes/
          unzip -l jp-diet-minutes.zip

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" jp-diet-minutes.zip \
            --title "$GITHUB_REF_NAME" \
            --notes-file .github/release-notes.md
